{{>header}}

<main class="d-flex justify-content-center flex-column">

    <div class="w-100 mt-2 mx-auto d-flex justify-content-center flex-wrap" id="cardsEl">

        {{#each countries}}
        {{>country}}
        {{/each}}

    </div>

</main>